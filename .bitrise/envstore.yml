envs:
- content: |
    #!/bin/bash
    set -v
    set -e
    bash ./_scripts/go_install_tools.sh
  is_expand: "false"
- is_expand: "false"
  runner_bin: /bin/bash
- is_debug: "no"
  is_expand: "false"
